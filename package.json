{
  "name": "madeclaw-online-server",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "MadeClaw official site + billing API + Waffo recharge (Railway-ready)",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
